import controller from './menu-list.controller';
import template from './menu-list.template.html';

const MenuListComponent = { 
  bindings: {},
  controller,
  templateUrl: template
};

export default MenuListComponent;